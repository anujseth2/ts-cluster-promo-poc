# Databricks warehouse credentials for scripts/seed_databricks.py ONLY.
# These are NOT the ThoughtSpot cluster creds (those live in .env).
# Copy to .env.seed, fill in, and `set -a; source .env.seed; set +a` before running.
# Rotate any PAT that has been shared in plaintext.

# ── TEST workspace (where the Sisense demo tables already live) ──────────────
TEST_DBX_HOST=dbc-c1944047-1496.cloud.databricks.com
TEST_DBX_HTTP_PATH=/sql/1.0/warehouses/b5f30d7c4aa8d54d
TEST_DBX_TOKEN=

# ── DEV workspace (the promotion SOURCE, needs the same tables) ──────────────
DEV_DBX_HOST=dbc-ba0cbe77-94ec.cloud.databricks.com
DEV_DBX_HTTP_PATH=/sql/1.0/warehouses/f6a4e6987957b8fb
DEV_DBX_TOKEN=
